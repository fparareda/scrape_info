name: Scrape · OAA (Ontario Architects)
on:
  schedule:
    - cron: "0 8 * * 0"
  workflow_dispatch:
jobs:
  run:
    uses: ./.github/workflows/_scrape-runner.yml
    secrets: inherit
    with:
      source: oaa
      timeout_minutes: 240
