name: Scrape · RQCI Quebec (CA extranjeria)
# ~682 records — one-shot, monthly.
on:
  schedule:
    - cron: "0 6 22 * *"
  workflow_dispatch:
jobs:
  run:
    uses: ./.github/workflows/_scrape-runner.yml
    secrets: inherit
    with:
      source: rqci-qc-ca
      timeout_minutes: 15
