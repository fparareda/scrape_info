name: Scrape · Arizona ROC
on:
  schedule:
    - cron: "0 2 * * 6"
  workflow_dispatch:
jobs:
  run:
    uses: ./.github/workflows/_scrape-runner.yml
    secrets: inherit
    with:
      source: arizona-roc
      timeout_minutes: 60
