name: Scrape · Graduados Sociales (ES federación)
on:
  schedule:
    - cron: "0 10 * * 1"
  workflow_dispatch:
jobs:
  run:
    uses: ./.github/workflows/_scrape-runner.yml
    secrets: inherit
    with:
      source: graduados-sociales-es
      timeout_minutes: 120
