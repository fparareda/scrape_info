name: Scrape · Missouri DPR
on:
  schedule:
    - cron: "0 16 * * 6"
  workflow_dispatch:
jobs:
  run:
    uses: ./.github/workflows/_scrape-runner.yml
    secrets: inherit
    with:
      source: missouri-dpr
      timeout_minutes: 60
