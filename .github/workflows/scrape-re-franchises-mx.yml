name: Scrape · re franchises mx
on:
  schedule:
    - cron: "0 10 12 * *" # Day 12 of each month, 10:00 UTC
  workflow_dispatch:
jobs:
  run:
    uses: ./.github/workflows/_scrape-runner.yml
    secrets: inherit
    with:
      source: re-franchises-mx
      timeout_minutes: 90
