name: Scrape · Open Data BCN — cens locals planta baixa (~70k locals)
on:
  schedule:
    - cron: "0 8 18 * *"
  workflow_dispatch:
jobs:
  run:
    uses: ./.github/workflows/_scrape-runner.yml
    secrets: inherit
    with:
      source: open-data-bcn-locales
      timeout_minutes: 60
