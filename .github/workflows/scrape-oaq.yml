name: Scrape · OAQ (Quebec architectes)
on:
  schedule:
    - cron: "0 3 * * 0"
  workflow_dispatch:
jobs:
  run:
    uses: ./.github/workflows/_scrape-runner.yml
    secrets: inherit
    with:
      source: oaq
      timeout_minutes: 240
