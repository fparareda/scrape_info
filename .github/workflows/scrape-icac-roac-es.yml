name: Scrape · icac roac es
# icac roac es — auto-wired icac-roac-es. One-shot, monthly.
on:
  schedule:
    - cron: "0 8 12 * *"
  workflow_dispatch:
jobs:
  run:
    uses: ./.github/workflows/_scrape-runner.yml
    secrets: inherit
    with:
      source: icac-roac-es
      timeout_minutes: 30
