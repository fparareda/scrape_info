name: Scrape · CNOP Pharmaciens (France)
on:
  schedule:
    - cron: "0 9 * * 6"
  workflow_dispatch:
jobs:
  run:
    uses: ./.github/workflows/_scrape-runner.yml
    secrets: inherit
    with:
      source: cnop-pharmaciens
      timeout_minutes: 90
