name: Scrape · cgcod es
# cgcod es — auto-wired cgcod-es. One-shot, monthly.
on:
  schedule:
    - cron: "0 8 6 * *"
  workflow_dispatch:
jobs:
  run:
    uses: ./.github/workflows/_scrape-runner.yml
    secrets: inherit
    with:
      source: cgcod-es
      timeout_minutes: 30
