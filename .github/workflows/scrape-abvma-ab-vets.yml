name: Scrape · ABVMA (AB vets)
on:
  schedule:
    - cron: "0 8 * * 0"
  workflow_dispatch:
jobs:
  run:
    uses: ./.github/workflows/_scrape-runner.yml
    secrets: inherit
    with:
      source: abvma-ab-vets
      timeout_minutes: 30
