name: Scrape · City of Vancouver Business Licences
on:
  schedule:
    - cron: "30 5 22 * *"
  workflow_dispatch:
jobs:
  run:
    uses: ./.github/workflows/_scrape-runner.yml
    secrets: inherit
    with:
      source: vancouver-business-licenses
      timeout_minutes: 90
