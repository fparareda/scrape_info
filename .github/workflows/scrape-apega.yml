name: Scrape · APEGA (AB engineers)
on:
  schedule:
    - cron: "0 9 * * 0"
  workflow_dispatch:
jobs:
  run:
    uses: ./.github/workflows/_scrape-runner.yml
    secrets: inherit
    with:
      source: apega
      timeout_minutes: 90
