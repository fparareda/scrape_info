name: Scrape · Architectes FR
on:
  schedule:
    - cron: "0 23 * * 6"
  workflow_dispatch:
jobs:
  run:
    uses: ./.github/workflows/_scrape-runner.yml
    secrets: inherit
    with:
      source: architectes-fr
      timeout_minutes: 90
