name: Scrape · US construccion RI
# US construccion RI — auto-wired rhode-island-crb. One-shot, monthly.
on:
  schedule:
    - cron: "0 7 10 * *"
  workflow_dispatch:
jobs:
  run:
    uses: ./.github/workflows/_scrape-runner.yml
    secrets: inherit
    with:
      source: rhode-island-crb
      timeout_minutes: 30
