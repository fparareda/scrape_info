name: Scrape · cpsnb nb physicians
# cpsnb nb physicians — auto-wired cpsnb-nb-physicians. One-shot, monthly.
on:
  schedule:
    - cron: "0 8 9 * *"
  workflow_dispatch:
jobs:
  run:
    uses: ./.github/workflows/_scrape-runner.yml
    secrets: inherit
    with:
      source: cpsnb-nb-physicians
      timeout_minutes: 30
