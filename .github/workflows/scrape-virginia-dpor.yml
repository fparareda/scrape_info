name: Scrape · Virginia DPOR
on:
  schedule:
    - cron: "0 9 * * 6"
  workflow_dispatch:
jobs:
  run:
    uses: ./.github/workflows/_scrape-runner.yml
    secrets: inherit
    with:
      source: virginia-dpor
      timeout_minutes: 60
