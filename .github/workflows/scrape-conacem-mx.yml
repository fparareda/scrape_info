name: Scrape · conacem mx
on:
  schedule:
    - cron: "0 14 18 * *" # Day 18 of each month, 14:00 UTC
  workflow_dispatch:
jobs:
  run:
    uses: ./.github/workflows/_scrape-runner.yml
    secrets: inherit
    with:
      source: conacem-mx
      timeout_minutes: 30
