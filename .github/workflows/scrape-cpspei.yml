name: Scrape · CPSPEI (PEI physicians)
on:
  schedule:
    - cron: "0 7 * * 0"
  workflow_dispatch:
jobs:
  run:
    uses: ./.github/workflows/_scrape-runner.yml
    secrets: inherit
    with:
      source: cpspei
      timeout_minutes: 90
