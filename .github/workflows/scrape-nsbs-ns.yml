name: Scrape · NSBS (Nova Scotia Barristers' Society)
on:
  schedule:
    - cron: "0 3 * * 0"
  workflow_dispatch:
jobs:
  run:
    uses: ./.github/workflows/_scrape-runner.yml
    secrets: inherit
    with:
      source: nsbs-ns
      timeout_minutes: 90
