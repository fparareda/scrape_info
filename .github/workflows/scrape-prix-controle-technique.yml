name: Scrape · Prix Contrôle Technique (France)
on:
  schedule:
    - cron: "0 6 * * 3"
  workflow_dispatch:
jobs:
  run:
    uses: ./.github/workflows/_scrape-runner.yml
    secrets: inherit
    with:
      source: prix-controle-technique
      timeout_minutes: 60
