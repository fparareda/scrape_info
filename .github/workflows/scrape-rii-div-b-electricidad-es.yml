name: Scrape · ES electricidad RII-B
# ES electricidad RII-B — auto-wired rii-div-b-electricidad-es. One-shot, monthly.
on:
  schedule:
    - cron: "0 7 11 * *"
  workflow_dispatch:
jobs:
  run:
    uses: ./.github/workflows/_scrape-runner.yml
    secrets: inherit
    with:
      source: rii-div-b-electricidad-es
      timeout_minutes: 45
