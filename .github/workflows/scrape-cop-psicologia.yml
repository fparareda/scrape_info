name: Scrape · COP psicología (ES federación)
on:
  schedule:
    - cron: "0 8 * * 1"
  workflow_dispatch:
jobs:
  run:
    uses: ./.github/workflows/_scrape-runner.yml
    secrets: inherit
    with:
      source: cop-psicologia
      timeout_minutes: 120
