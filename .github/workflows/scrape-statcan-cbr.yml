name: Scrape · Statistics Canada Business Counts (CBR)
on:
  schedule:
    - cron: "10 5 22 * *"
  workflow_dispatch:
jobs:
  run:
    uses: ./.github/workflows/_scrape-runner.yml
    secrets: inherit
    with:
      source: statcan-cbr
      timeout_minutes: 60
