name: Scrape · ADEME RGE (France)
on:
  schedule:
    - cron: "0 4 * * 1"
  workflow_dispatch:
jobs:
  run:
    uses: ./.github/workflows/_scrape-runner.yml
    secrets: inherit
    with:
      source: ademe-rge
      timeout_minutes: 90
