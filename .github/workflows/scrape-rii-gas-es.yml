name: Scrape · rii gas es
# rii gas es — auto-wired rii-gas-es. One-shot, monthly.
on:
  schedule:
    - cron: "0 8 10 * *"
  workflow_dispatch:
jobs:
  run:
    uses: ./.github/workflows/_scrape-runner.yml
    secrets: inherit
    with:
      source: rii-gas-es
      timeout_minutes: 30
