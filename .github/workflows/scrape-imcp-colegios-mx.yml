name: Scrape · IMCP colegios contadores (MX)
on:
  schedule:
    - cron: "0 6 1 * *"   # monthly 1st, 06:00 UTC
  workflow_dispatch:
jobs:
  run:
    uses: ./.github/workflows/_scrape-runner.yml
    secrets: inherit
    with:
      source: imcp-colegios-mx
      timeout_minutes: 60
