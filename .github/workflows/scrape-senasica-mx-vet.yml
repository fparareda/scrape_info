name: Scrape · SENASICA MX Veterinarios
on:
  schedule:
    - cron: "0 14 * * 0" # Sunday 14:00 UTC weekly
  workflow_dispatch:
jobs:
  run:
    uses: ./.github/workflows/_scrape-runner.yml
    secrets: inherit
    with:
      source: senasica-mx-vet
      timeout_minutes: 60
