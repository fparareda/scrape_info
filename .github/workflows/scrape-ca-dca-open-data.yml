name: Scrape · California DCA Open Data
on:
  schedule:
    - cron: "0 5 22 * *"
  workflow_dispatch:
jobs:
  run:
    uses: ./.github/workflows/_scrape-runner.yml
    secrets: inherit
    with:
      source: ca-dca-open-data
      timeout_minutes: 240
