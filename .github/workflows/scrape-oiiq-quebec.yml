name: Scrape · oiiq-quebec
on:
  schedule:
    - cron: "0 7 * * 0"
  workflow_dispatch:
jobs:
  run:
    uses: ./.github/workflows/_scrape-runner.yml
    secrets: inherit
    with:
      source: oiiq-quebec
      timeout_minutes: 60
