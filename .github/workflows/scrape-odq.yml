name: Scrape · ODQ (Quebec dentistes)
on:
  schedule:
    - cron: "0 2 * * 0"
  workflow_dispatch:
jobs:
  run:
    uses: ./.github/workflows/_scrape-runner.yml
    secrets: inherit
    with:
      source: odq
      timeout_minutes: 90
