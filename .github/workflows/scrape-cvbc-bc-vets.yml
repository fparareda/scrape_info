name: Scrape · cvbc bc vets
# cvbc bc vets — auto-wired cvbc-bc-vets. One-shot, monthly.
on:
  schedule:
    - cron: "0 8 10 * *"
  workflow_dispatch:
jobs:
  run:
    uses: ./.github/workflows/_scrape-runner.yml
    secrets: inherit
    with:
      source: cvbc-bc-vets
      timeout_minutes: 30
