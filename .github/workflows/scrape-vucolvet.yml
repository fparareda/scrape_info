name: Scrape · VUCOLVET (España veterinarios nacional)
on:
  schedule:
    - cron: "0 11 * * 0"
  workflow_dispatch:
jobs:
  run:
    uses: ./.github/workflows/_scrape-runner.yml
    secrets: inherit
    with:
      source: vucolvet
      timeout_minutes: 120
