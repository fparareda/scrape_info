name: Scrape · CA fisioterapia ON
# CA fisioterapia ON — auto-wired cpo-on-physio. One-shot, monthly.
on:
  schedule:
    - cron: "0 7 7 * *"
  workflow_dispatch:
jobs:
  run:
    uses: ./.github/workflows/_scrape-runner.yml
    secrets: inherit
    with:
      source: cpo-on-physio
      timeout_minutes: 30
