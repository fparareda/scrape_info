name: Scrape · CSCAE (España arquitectos nacional)
on:
  schedule:
    - cron: "0 10 * * 0"
  workflow_dispatch:
jobs:
  run:
    uses: ./.github/workflows/_scrape-runner.yml
    secrets: inherit
    with:
      source: cscae
      timeout_minutes: 120
