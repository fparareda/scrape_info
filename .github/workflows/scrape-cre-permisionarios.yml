name: Scrape · cre permisionarios
on:
  schedule:
    - cron: "0 5 3 * *" # Day 3 of each month at 05:00 UTC
  workflow_dispatch:
jobs:
  run:
    uses: ./.github/workflows/_scrape-runner.yml
    secrets: inherit
    with:
      source: cre-permisionarios
      timeout_minutes: 90
