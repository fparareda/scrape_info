name: Scrape · rii div a talleres es
# rii div a talleres es — auto-wired rii-div-a-talleres-es. One-shot, monthly.
on:
  schedule:
    - cron: "0 8 8 * *"
  workflow_dispatch:
jobs:
  run:
    uses: ./.github/workflows/_scrape-runner.yml
    secrets: inherit
    with:
      source: rii-div-a-talleres-es
      timeout_minutes: 30
