name: Scrape · SCPP (SK pharmacists)
on:
  schedule:
    - cron: "0 8 * * 0"
  workflow_dispatch:
jobs:
  run:
    uses: ./.github/workflows/_scrape-runner.yml
    secrets: inherit
    with:
      source: scpp-sk-pharmacists
      timeout_minutes: 60
