name: Scrape · FINESS (France)
on:
  schedule:
    - cron: "0 5 * * 2"
  workflow_dispatch:
jobs:
  run:
    uses: ./.github/workflows/_scrape-runner.yml
    secrets: inherit
    with:
      source: finess
      timeout_minutes: 90
