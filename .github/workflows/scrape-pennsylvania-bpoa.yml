name: Scrape · Pennsylvania BPOA
on:
  workflow_dispatch:
jobs:
  run:
    uses: ./.github/workflows/_scrape-runner.yml
    secrets: inherit
    with:
      source: pennsylvania-bpoa
      timeout_minutes: 60
