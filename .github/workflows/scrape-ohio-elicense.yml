name: Scrape · Ohio eLicense
on:
  schedule:
    - cron: "0 17 * * 6"
  workflow_dispatch:
jobs:
  run:
    uses: ./.github/workflows/_scrape-runner.yml
    secrets: inherit
    with:
      source: ohio-elicense
      timeout_minutes: 60
