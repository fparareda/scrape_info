name: Scrape · CGCFE fisioterapia (ES federación)
on:
  schedule:
    - cron: "0 7 * * 1"
  workflow_dispatch:
jobs:
  run:
    uses: ./.github/workflows/_scrape-runner.yml
    secrets: inherit
    with:
      source: cgcfe-fisio
      timeout_minutes: 120
