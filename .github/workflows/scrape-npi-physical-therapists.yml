name: Scrape · npi physical therapists
# npi physical therapists — auto-wired npi-physical-therapists. One-shot, monthly.
on:
  schedule:
    - cron: "0 8 4 * *"
  workflow_dispatch:
jobs:
  run:
    uses: ./.github/workflows/_scrape-runner.yml
    secrets: inherit
    with:
      source: npi-physical-therapists
      timeout_minutes: 30
