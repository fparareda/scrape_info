name: Scrape · AMVIC (AB auto trades)
on:
  schedule:
    - cron: "0 9 * * 1"
  workflow_dispatch:
jobs:
  run:
    uses: ./.github/workflows/_scrape-runner.yml
    secrets: inherit
    with:
      source: amvic-dealers
      timeout_minutes: 90
