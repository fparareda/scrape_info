name: Scrape · mda mb dentists
# mda mb dentists — auto-wired mda-mb-dentists. One-shot, monthly.
on:
  schedule:
    - cron: "0 9 5 * *"
  workflow_dispatch:
jobs:
  run:
    uses: ./.github/workflows/_scrape-runner.yml
    secrets: inherit
    with:
      source: mda-mb-dentists
      timeout_minutes: 30
