name: Scrape · Tennessee TDCI
on:
  workflow_dispatch:
jobs:
  run:
    uses: ./.github/workflows/_scrape-runner.yml
    secrets: inherit
    with:
      source: tennessee-tdci
      timeout_minutes: 60
