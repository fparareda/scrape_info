name: Scrape · nebraska ea
# nebraska ea — auto-wired nebraska-ea. One-shot, monthly.
on:
  schedule:
    - cron: "0 8 20 * *"
  workflow_dispatch:
jobs:
  run:
    uses: ./.github/workflows/_scrape-runner.yml
    secrets: inherit
    with:
      source: nebraska-ea
      timeout_minutes: 30
