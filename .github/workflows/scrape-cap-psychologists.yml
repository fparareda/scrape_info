name: Scrape · CAP (AB psychologists)
on:
  schedule:
    - cron: "0 8 * * 0"
  workflow_dispatch:
jobs:
  run:
    uses: ./.github/workflows/_scrape-runner.yml
    secrets: inherit
    with:
      source: cap-psychologists
      timeout_minutes: 90
