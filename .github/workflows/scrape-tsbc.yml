name: Scrape · TSBC (BC trades)
on:
  schedule:
    - cron: "0 7 * * 1"
  workflow_dispatch:
jobs:
  run:
    uses: ./.github/workflows/_scrape-runner.yml
    secrets: inherit
    with:
      source: tsbc
      timeout_minutes: 90
