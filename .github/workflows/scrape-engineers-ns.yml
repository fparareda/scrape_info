name: Scrape · Engineers Nova Scotia
on:
  schedule:
    - cron: "0 2 * * 0"
  workflow_dispatch:
jobs:
  run:
    uses: ./.github/workflows/_scrape-runner.yml
    secrets: inherit
    with:
      source: engineers-ns
      timeout_minutes: 120
