name: Scrape · cofepris farmacias
on:
  schedule:
    - cron: "0 5 6 * *" # Day 6 of each month at 05:00 UTC
  workflow_dispatch:
jobs:
  run:
    uses: ./.github/workflows/_scrape-runner.yml
    secrets: inherit
    with:
      source: cofepris-farmacias
      timeout_minutes: 90
