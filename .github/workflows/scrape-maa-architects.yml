name: Scrape · maa architects
# maa architects — auto-wired maa-architects. One-shot, monthly.
on:
  schedule:
    - cron: "0 8 16 * *"
  workflow_dispatch:
jobs:
  run:
    uses: ./.github/workflows/_scrape-runner.yml
    secrets: inherit
    with:
      source: maa-architects
      timeout_minutes: 30
