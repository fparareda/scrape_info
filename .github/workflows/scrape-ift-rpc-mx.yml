name: Scrape · IFT RPC (MX telecomunicaciones)
on:
  schedule:
    - cron: "0 5 1 * *"   # monthly 1st, 05:00 UTC
  workflow_dispatch:
jobs:
  run:
    uses: ./.github/workflows/_scrape-runner.yml
    secrets: inherit
    with:
      source: ift-rpc-mx
      timeout_minutes: 60
